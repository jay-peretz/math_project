/*global describe, it */

describe('<ms-button-group>', function () {
    it('should change the expected property of the data object passed with answer events'); // 1 expect
    it('should stop checkAnswer events coming down and $emit answer events with a proper data object'); // 3 expects
});
