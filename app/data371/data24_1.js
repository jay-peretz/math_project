/*global angular */

angular.module('mathSkills').service('data24_1', ['dataUtils', function (dataUtils) {
    var desc = {
			title: '24.1 Simplifying Measurements in the English System',
            path: '24.1-simplifying-measurements-in-the-english-system',
            children: []
        },
		template = {
        },
        data = [
        ];

    return dataUtils.build(desc, template, data);
}]);
